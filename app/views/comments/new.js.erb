$("#<%= "reply-form-#{@parent.id}" %>").html("<%= escape_javascript(render 'comments/reply_form', comment: @comment, parent_id: @parent.id) %>");
$("#<%= "reply-form-#{@parent.id}" %>").slideDown(1200);
$("#<%= "reply-form-#{@parent.id}" %> textarea").focus()