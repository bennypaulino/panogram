<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <!-- <div class="pagination primary-font"> -->
    <!-- The controller will determine how many posts/page --> 
    <%#= will_paginate @feed_items, previous_label: "<", 
                                   next_label: ">",
                                   inner_window: 1, 
                                   outer_window: 1,
                                   :container => false %>
  <!-- </div> -->
<% end %>