<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1 class="primary-font"><%= current_user.username %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span class="sidebar-post-count"><%= pluralize(current_user.microposts.count, "pano-post") %></span>