// Update the heart icon and the number of likes for a micropost
$("#like_form_<%= @micropost.id %>").html("<%= escape_javascript(render('microposts/dislike', micropost: @micropost)) %>");
$("#admirers_<%= @micropost.id %>").html("<%= pluralize(@micropost.likes.count, 'like') %>");
